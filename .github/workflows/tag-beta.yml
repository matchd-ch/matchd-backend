on:
  push:
    tags:
      - "v[12].[0-9]+.[0-9]+-beta.[0-9]+"

name: "Beta Tag"

jobs:
  build:
    name: Build

    uses: matchd-ch/matchd-backend/.github/workflows/part_build.yml@main

  deploy:
    name: "Deploy"

    needs: ['build']

    uses: matchd-ch/matchd-backend/.github/workflows/part_deploy.yml@main
    with:
      namespace: matchd-backend-staging
      release: matchd-backend
      environment: staging
      domain: staging-matchd-backend.joshmartin.ch
      version: "${{ needs.build.outputs.tag }}"
      dbHost: 10.100.35.5
      dbUser: matchd-staging
      dbName: matchd-staging
      settingsModule: app.settings.production
      senderEmail: info@matchd.ch
      smtpRelay: smtp.postmarkapp.com
      smtpPort: 2525
      emailSubjectPrefix: '[STAGING]'
      frontendUrl: https://staging-matchd-frontend.joshmartin.ch
      csrfCookieDomain: '.joshmartin.ch'
      dataProtectionUrl: 'https://example.com'
      resetDb: false
    secrets:
      DOCKER_PULL_USERNAME: ${{ secrets.DOCKER_PULL_USERNAME }}
      DOCKER_PULL_SECRET: ${{ secrets.DOCKER_PULL_SECRET }}
      DB_PASSWORD: ${{ secrets.STAGING_DB_PASSWORD }}
      SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
      SMTP_USER: ${{ secrets.SMTP_USER }}
      SECRET_KEY: ${{ secrets.STAGING_SECRET_KEY }}