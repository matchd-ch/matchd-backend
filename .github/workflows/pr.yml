on:
  pull_request:
    branches:
      - "*"

name: "Pull Request"

jobs:
  test:
    name: "Test"

    uses: matchd-ch/matchd-backend/.github/workflows/part_test.yml@feat/sentry_integration
  
  build:
    name: "Build"

    uses: matchd-ch/matchd-backend/.github/workflows/part_build.yml@feat/sentry_integration
  
  dependabot:
    name: "Dependabot"

    needs: ['test', 'build']

    uses: matchd-ch/matchd-backend/.github/workflows/part_dependabot.yml@feat/sentry_integration